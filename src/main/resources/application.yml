spring:

  application:
    name: shoppingCard

  main:
    banner-mode: off
  datasource:
#    url: jdbc:mysql://localhost:3306/shopping_cart
#    driverClassName: com.mysql.cj.jdbc.Driver
#    username: root
#    password: root

    username: postgres
    password: root
    url: jdbc:postgresql://postgres:5432/shopping_cart?currentSchema=shopping_cart
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: shopping_cart
  flyway:
    schemas: shopping_cart
    baselineOnMigrate: true
    enabled: false

secret: shoppingCart
server:
  port: 80
  address: 0.0.0.0