spring:

  application:
    name: shoppingCard

  main:
    banner-mode: off
  datasource:
#    url: jdbc:mysql://localhost:3306/shopping_cart
#    driverClassName: com.mysql.cj.jdbc.Driver
#    username: root
#    password: root

    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/shopping_cart?currentSchema=shopping_cart
    driverClassName: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: shopping_cart
  flyway:
    schemas: shopping_cart
    baselineOnMigrate: true
    enabled: true

secret: shoppingCart
server:
  port: 80
  address: 0.0.0.0

#cloud:
#  aws:
#    region:
#      auto: false
#      static: us-east-1
#      use-default-aws-region-chain: true
#    credentials:
#      use-default-aws-credentials-chain: true
#    stack:
#      auto: false
#
cloud:
  aws:
    region:
      auto: false
      static: eu-west-1
      use-default-aws-region-chain: true
    stack: false
    stack.auto: false
logging:
  level:
    com:
      amazonaws:
        internal:
          InstanceMetadataServiceResourceFetcher: error
        util:
          EC2MetadataUtils: error